# Prototype Storyboard Instructions

<workflow>
  <step n="1" goal="Define user scenario">
    <action>Pick persona, context, and trigger event.</action>
  </step>
  <step n="2" goal="Outline scenes">
    <action>Describe current state pain, the AI-enabled interaction, and desired future state.</action>
    <action>Capture emotion and environment per scene.</action>
  </step>
  <step n="3" goal="Highlight system touchpoints">
    <action>Map channels, data inputs, and system responses.</action>
    <action>Note offline or low-connectivity fallbacks.</action>
  </step>
  <step n="4" goal="Document storyboard">
    <action>Fill template, include visuals references, and export to {output_folder}/storyboards.</action>
  </step>
</workflow>
